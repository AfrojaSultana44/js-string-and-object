// Check whether a string contains all the vowels a, e, i, o, u

const str = "i eat rice very much with boiled pottatto";
const newStr = str.includes("a" && "e" && "i" && "o" && "u");
console.log(newStr);
